<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:enabled="true"
        android:icon="@drawable/shopping"
        android:shortcutDisabledMessage="@string/btn_offline_transaction"
        android:shortcutId="static"
        android:shortcutLongLabel="@string/btn_offline_transaction"
        android:shortcutShortLabel="@string/btn_offline_transaction">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetClass="trioidea.iciciappathon.com.trioidea.Activities.MainScreen"
            android:targetPackage="trioidea.iciciappathon.com.trioidea" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>